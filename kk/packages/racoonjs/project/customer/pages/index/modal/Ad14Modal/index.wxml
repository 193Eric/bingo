<modal compid="{{$compid__37}}" wx:if="{{$taroCompReady}}">
    <view class="promotion-activity-carousel">
        <image bindtap="handleImageClick" data-e-tap-so="this" src="{{imgUrl}}"></image>
    </view>
</modal>
