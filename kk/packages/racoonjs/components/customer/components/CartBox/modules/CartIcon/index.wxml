<view bindtap="openCartPage" class="{{'new_cart_icon '+cartIconActiveClass}}" data-e-tap-so="this" id="new_cart_icon" wx:if="{{$taroCompReady}}">
    <view class="cart_product_num" wx:if="{{totalCount>0}}">{{totalCount}}</view>
</view>
